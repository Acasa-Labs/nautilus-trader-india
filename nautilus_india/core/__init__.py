"""Broker-neutral Indian market truth: symbology, instruments, lots, calendar, costs.

Nothing in this package talks to a network or knows a broker exists. That is
enforced by `tests/test_package.py`, not by convention.
"""
